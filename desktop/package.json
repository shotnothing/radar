{
  "name": "radar-desktop",
  "version": "0.1.0",
  "description": "Radar desktop app",
  "private": true,
  "type": "module",
  "scripts": {
    "tauri:dev": "tauri dev",
    "tauri:build": "tauri build",
    "frontend:dev": "npm --prefix frontend run dev",
    "frontend:build": "npm --prefix frontend run build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.2"
  }
}
